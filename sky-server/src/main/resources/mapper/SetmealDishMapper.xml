<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <select id="getSetMealIdsByDishIds" resultType="long">
        select setmeal_id from setmeal_dish where dish_id in
                                 <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
                                     #{dishId}
                                 </foreach>
    </select>

    <select id="getDishIdsBySetmealIds" resultType="long">
        select dish_id from setmeal_dish where setmeal_id in
                                         <foreach collection="ids" item="id" separator="," close=")" open="(">
                                             #{id}
                                         </foreach>

    </select>

    <delete id="deleteBatchBySetmealIds">
        delete from setmeal_dish where setmeal_id in
                                 <foreach collection="setmealIds" item="setmealId" separator="," open="(" close=")">
                                     #{setmealId}
                                 </foreach>
    </delete>

    <select id="getDishItemVOBySetmealId" resultType="com.sky.vo.DishItemVO">
        select sd.copies as copies , d.name as name , d.image as image , d.description as description
        from setmeal_dish sd left outer join dish d on sd.dish_id = d.id
        where sd.setmeal_id = #{id}
    </select>
</mapper>
